[user]
	name = {{ .git_name }}
	email = {{ .git_email }}

[alias]
	b = !f() { command -v fzf &>/dev/null || { echo 'fzf not installed' >&2; return 1; }; git for-each-ref --format='%(refname:short)' refs/heads/ | fzf +s +m -e | xargs -r git checkout; }; f
